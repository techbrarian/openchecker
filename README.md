openchecker
===========

This is a simple javascript utility designed to check an institution's hours along with dates and holidays to see if it is open. The open/closed notification is sent as innerhtml to a DOM div. Requires jQuery.
